export const add = (a, b) => a + b;
export const subtract = (a, b) => a - b;

export default function greet(nama) {
  console.log(`Hello ${nama} !!`);
}

// harus ada satu export default
// export default function greeting(nama) {
//   console.log(`Hai ${nama} !!`);
// }
