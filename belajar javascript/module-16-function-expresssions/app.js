// FUnction Expressions

// Selain membuat function menggunakan deklarasi (function declaration), Anda juga bisa membuat function menggunakan eskpresi (function expression), di mana function disimpan dalam variabel.

let kali = function (a, b) {
  return a * b;
};

console.log(kali(4, 5));
